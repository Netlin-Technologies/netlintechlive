// Minimal placeholder to satisfy imports from sitemap.ts

export type UseCase = {
  slug: string;
  updatedAt?: string | number | Date | null;
};

export const listUseCases = (): UseCase[] => {
  // Return an empty list for now; replace with real entries if/when available.
  return [];
};